// Package tracker is the Quests-owned copy of questmaster's tui tracker, so its
// UI can diverge without affecting the frozen questmaster binary.
// It renders every known questmaster session in one hierarchy with per-session detail
// and a 3-second polling cadence.
package tracker
