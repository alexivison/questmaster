// Package tui provides the shared Bubble Tea TUI foundation for party-cli.
// It supports auto-selection between worker sidebar mode and master tracker mode,
// with width-adaptive rendering and a 3-second polling cadence.
package tui
